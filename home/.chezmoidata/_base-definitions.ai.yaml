_base:
  definitions:
    featureFlags:
      ## Enable Claude app configuration file generation
      ai::claude:enable:
      ## Enable Gemini app configuration file generation
      ai::gemini:enable:
