{{- includeTemplate "featureFlags/helpers/mustEnable" (dict
  "flag" "zinit::plugins:install"
  "context" $
) -}}

#!/usr/bin/env bash

set -euo pipefail

printf '>> Starting... %s\n' "featureflags-zinit.sh"
zsh -ic -- "@zinit-scheduler burst"
