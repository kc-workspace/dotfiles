{{- /* Define this on first line to abort file creation */ -}}
{{- /* if featureFlags is NOT enabled                   */ -}}
{{- includeTemplate "featureFlags/internal/validate" (dict
  "flag" .flag
  "context" .context
) -}}
{{- if includeTemplate "featureFlags/internal/isDisable" (dict
  "flag" .flag
  "context" .context
) | mustFromJson -}}
  {{- abortEmpty -}}
{{- end -}}
