{{- /* Define this on first line to abort file creation */ -}}
{{- /* if featureFlags is NOT enabled                   */ -}}
{{- if includeTemplate "featureFlags/helpers/isDisable" (dict
  "flag" .flag
  "context" .context
) | mustFromJson -}}
  {{- abortEmpty -}}
{{- end -}}
